custom-environment-variables.json

...allows you to map the password to the mail server
without declaring it in your config files. You set this 
in the console with the command: 
    set app_password=<password>
not working at the mo as can't seem to set passwords in bash